import { createContext } from "react";
const noteContext = createContext();

export default noteContext;
// in this only that i talking to react that i want to use context api default exort can use any name in other while importimng and not with other defauts
